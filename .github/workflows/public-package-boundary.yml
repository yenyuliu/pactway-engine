name: Public package boundary

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  public-package-boundary:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Test
        run: npm test

      - name: Public release gate
        run: npm run release:public-check
